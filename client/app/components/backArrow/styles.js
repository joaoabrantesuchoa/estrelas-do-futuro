import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  backContainer: {
    position: "absolute",
    top: 0,
    left: 0,
  },
  button: {
    flexDirection: "row",
    alignItems: "center",
  },
  backArrow: {
    width: 30,
    height: 30,
  },
  text: {
    fontSize: 20,
  },
});
