import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignContent: "center",
    justifyContent: "center",
    padding: 40,
  },
  button: {
    width: 100,
    height: 50,
  },
  containerText: {
    color: "#000",
    textAlign: "center",
    fontSize: 20,
    fontWeight: "400",
    lineHeight: 24,
    paddingRight: 10,
  },
  containerRol: {
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "row",
  },

  monthList: {
    maxWidth: "auto",
    maxHeight: "80%",
    marginBottom: 10,
  },
});
