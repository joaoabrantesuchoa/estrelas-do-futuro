import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 40,
  },
  containerButton: {
    flexDirection: "row",
    alignItems: "center",
    paddingTop: 30,
  },
});
