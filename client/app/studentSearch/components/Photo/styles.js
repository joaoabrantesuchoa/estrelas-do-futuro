import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  circuloContainer: {
    width: "50px",
    height: "50px",
    flexShrink: 0,
    borderRadius: "54px",
    backgroundColor: "#D9D9D9",
    marginRight: 10,
    margin: 5,
  },
});
