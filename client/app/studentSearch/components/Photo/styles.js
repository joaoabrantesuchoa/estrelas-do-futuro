import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  circuloContainer: {
    width: "54px",
    height: "54px",
    flexShrink: 0,
    borderRadius: "54px",
    backgroundColor: "#D9D9D9",
  },
});
